jsx.jsx


Javascript XML -(jsx);
	
	jsx tag name has tagname,attribute,children.

	jsx is not a nessicity of progmram but it makes your code elegant, easy to understand, simple.

	jsx ultimatly transpile to js code which is understood by browsers.

	

	PRobs :
this are the objects which are passed by parent element to the functions/class.
in class we can access them using this.probs .
these probs propertiies are immutable.

this keyword is undifined in event handler this is why we need to do event binding.


KEY :
A key is a special string attribute which you need to include while creating a list element.
keys give elements a stable identity.
key helps react to identify which item is modiefied, added or removed.
helps in efficient updateof the user interface.